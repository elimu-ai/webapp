<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- https://docs.spring.io/spring-security/site/docs/4.0.0.RELEASE/reference/html/ns-config.html -->

    <http>
        <headers>
            <frame-options policy="SAMEORIGIN" />
        </headers>
        
        <csrf disabled="true" />
        
        <form-login login-page="/sign-on" authentication-failure-url="/sign-on?error" />

        <intercept-url pattern="/admin/**" access="hasRole('ADMIN')" />
        <intercept-url pattern="/analytics/**" access="hasRole('ANALYST')" />
        <intercept-url pattern="/content/**" access="hasRole('CONTRIBUTOR')" />

        <logout />
    </http>

    <authentication-manager />
</beans:beans>
